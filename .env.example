# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# AUTH_URL=http://localhost:3000/api/auth
AUTH_SECRET="" # Linux: `openssl rand -hex 32` or go to https://generate-secret.now.sh/32
# AUTH_REDIRECT_PROXY_URL=https://dualinsa.vercel.app/api/auth # Used for OAuth2 redirect but doesn't work for some reason

# Connect to Supabase via connection pooling with Supavisor.
POSTGRES_URL=""


# Direct connection to the database. Used for migrations.
POSTGRES_PRISMA_URL=""


# GROQ API key
GROQ_API_KEY=""

# OAuth2 (from INSA Apereo CAS server)
OAUTH_CLIENT_ID=""
OAUTH_CLIENT_SECRET=""